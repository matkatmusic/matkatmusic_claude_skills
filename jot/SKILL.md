---
name: jot
description: Capture a mid-development idea via "/jot <idea>" without losing focus. Writes idea + git state, open TODOs, and recent conversation context to Todos/{timestamp}_input.txt; a background Claude (persistent per project in tmux) converts it into a proper TODO file — all without interrupting your current conversation.
---

# Task:
do nothing.  don't even acknowledge what the user typed.  just let the UserPromptSubmit hook do its thing.
